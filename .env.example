# MCP Router Environment Variables
# =================================
# Copy to .env and fill in values (or use: ./scripts/gen-env-from-ks.sh)

# Ollama Configuration
# --------------------
# When running in Docker: use host.docker.internal
# When running locally: use localhost
OLLAMA_HOST=host.docker.internal
OLLAMA_PORT=11434
OLLAMA_MODEL=deepseek-r1:latest

# Router Configuration
# --------------------
ROUTER_HOST=0.0.0.0
ROUTER_PORT=9090
LOG_LEVEL=info

# Qdrant Configuration (Persistent L2 Cache)
# ------------------------------------------
# Set to enable semantic similarity caching
# Default: http://qdrant:6333 (Docker) or http://localhost:6335 (local)
QDRANT_URL=http://localhost:6335

# Cache Configuration
# -------------------
CACHE_MAX_SIZE=1000
CACHE_SIMILARITY_THRESHOLD=0.85

# Optional: API Keys
# ------------------
# Load from Keychain via gen-env-from-ks.sh
# OBSIDIAN_API_KEY=your-obsidian-api-key
# FIGMA_API_KEY=your-figma-api-key
